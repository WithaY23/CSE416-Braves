# Code Review Report — Final Presentation

**Date:** 2026-05-14
**Branch:** main
**Reviewer:** Claude (automated via /code-review skill)
**Scope:** All 43 implemented use cases — 20 GUI, 11 Preprocessing, 12 SeaWulf

---

## Presentation Prep Summary

**Walkthrough Order (follow the sidebar top-to-bottom):**

**1. Splash Page** — `SplashPage.jsx` → `/`
- GUI-1: Click a state on the US map to select it. Mention data comes from SeaWulf/preprocessing (not demoed live).
- GUI-23: Click "VRA Repeal Analysis" title at any point to return here (reset).

**2. State Data Summary** — `StatePage.jsx` → `/state/:stateName`
- GUI-2: District map renders from TopoJSON served via `GeometryAssetService`.
- GUI-3: State summary table (note: `groupRoughProportionality` field is empty — Prepro-12 gap).
- GUI-4: Toggle to Precinct Heat Map via sidebar. Real data, renders correctly for both states.
- GUI-6: Congressional representation table. District fields are real; effectiveness scores are placeholder values (Prepro-13/14 gap).
- GUI-7: Click a row in the district table to highlight that district on the map.

**3. Interesting District Plans** — `Compare.jsx` → `/state/:stateName/Compare`
- GUI-8: Compare two district plans side-by-side. *(Note: uses hardcoded SC test data; only SC works.)*
- GUI-19: Interesting plan map displayed alongside comparison. *(Note: also hardcoded SC data.)*

**4. Gingles** — `Gingles.jsx` → `/state/:stateName/Gingles`
- GUI-9: Scatter plot with regression curves. **Real preprocessing output for both OR and SC.**
- GUI-10: Precinct-level Gingles table, paginated. **Real data, both states.**

**5. Ecological Inferences** — `EI.jsx` → `/state/:stateName/Ecological Inference`
- GUI-12: EI support distribution density curves. *(Synthetic seed data; real PyEI output not yet wired.)*
- GUI-13: Precinct bar chart + confidence intervals. *(Synthetic seed data.)*
- GUI-15: EI KDE results. *(Synthetic seed data.)*

**6. Simulation Data** — `Simulation.jsx` → `/state/:stateName/Simulation Data`
- GUI-16 (Ensemble Splits): Bar chart comparing race-blind vs. VRA-constrained district seat splits. *(Synthetic seed data.)*
- GUI-17 (Box & Whisker): Box plots for minority VAP % across ensemble plans. *(Synthetic seed data.)*
- GUI-21 (ME Box & Whisker): Minority effectiveness box plots, rb vs. vra merged. *(Synthetic seed data.)*
- GUI-22 (ME Histogram): Minority effectiveness histogram. *(Synthetic seed data.)*
- GUI-20 (VRA Impact Threshold Table): Threshold comparison table. *(Synthetic seed data.)*
- GUI-26 (ME & Majority-Minority Bar Charts): *(Hardcoded constant — no API call; known gap.)*

**Data provenance note (for professor's document):**
All ensemble, EI, and VRA impact data was generated by our SeaWulf and preprocessing pipelines and seeded into MongoDB. Gingles (GUI-9/10) uses real preprocessing output; all other analysis tabs use synthetic fixtures shaped to the same schema.

---

## Executive Summary

| Category | Implementing | PASS | PARTIAL | MISSING |
|----------|-------------|------|---------|---------|
| GUI (20 total) | 1–4, 6–10, 12–13, 15–17, 19–23, 26 | 14 | 6 | 0 |
| Preprocessing (11 total) | 1–9, 11–12 | 9 | 1 | 1 |
| SeaWulf (12 total) | 1–11, 13 | 11 | 1 | 0 |

**Mock vs. Real Data Summary (GUI):**
- **Real preprocessing output (fully migrated):** GUI-9, GUI-10
- **Hardcoded JS constants (never call an API):** GUI-26, GUI-8, GUI-19
- **Seeded synthetic fixtures (call real API; data not yet from SeaWulf/PyEI):** GUI-6 (effectiveness scores), GUI-12, GUI-13, GUI-15, GUI-16, GUI-17, GUI-20, GUI-21, GUI-22

**Top 3 Most Urgent Issues:**
1. `Simulation.jsx:447–459` — `MinorityEffectiveDistrictsBar` and `MajorityMinorityDistrictsBar` use a **hardcoded constant** (`minorityEffectiveDistsData`) instead of calling the API. GUI-26 is a required use case and is PARTIAL.
2. `Compare.jsx:185,258` — Entire Compare page uses **3 hardcoded local JS files** and a hardcoded `testPlanList`. `useInterestingPlan` is commented out. GUI-8 and GUI-19 are blocked.
3. `stateUtils.js:20` — `groupOptionsForState` returns only `['Latino']` for OR and `['Black']` for SC, **silently blocking all other group data** from every analysis page.

---

## GUI Use Case Coverage Matrix

| ID | Name | Priority | Status | Data Source | Notes |
|----|------|----------|--------|-------------|-------|
| GUI-1 | Select state to display | Required | **PASS** | REAL | `/api/states` + `useUsStatesTopology`. Both OR/SC. |
| GUI-2 | Display enacted district map | Required | **PASS** | REAL | Classpath TopoJSON via `GeometryAssetService`. Both states. |
| GUI-3 | State data summary | Required | **PARTIAL** | REAL | Fetches from `/api/states/{stateId}/state-summary`. `groupRoughProportionality` field empty (`StatePage.jsx:112`) — Prepro-12 gap. |
| GUI-4 | Demographic heat map | Required | **PASS** | REAL | `useHeatmap` fetches real API bins. Legend uses hardcoded `grades` but rendering is correct and functional for both states. |
| GUI-6 | Congressional representation table | Required | **PARTIAL** | **MOCK-SEED** | Fetches from `/api/states/{stateId}/districts/enacted/table`. `effectivenessScore` and `calibratedEffectivenessScore` are **fake placeholder values** — Prepro-13/14 gap. Other district fields are real. |
| GUI-7 | Highlight district | Preferred | **PASS** | N/A | Click-to-highlight in district table working for both states. |
| GUI-8 | Compare two district plans | Preferred | **PARTIAL** | **MOCK** | Uses hardcoded `SCCurrentDistrictData`, `SCPackingData`, `SCCrackingData` local JS files. `useInterestingPlan` commented out at line 112. Only works for SC. |
| GUI-9 | Gingles scatter plot | Required | **PASS** | **REAL** | Real preprocessing output for OR/SC. Regression curves rendered. Both states. |
| GUI-10 | Gingles precinct table | Preferred | **PASS** | **REAL** | Real preprocessing output. Paginated. Both states. |
| GUI-12 | EI support distribution | Required | **PASS** | MOCK-SEED | Synthetic fixtures in `mock-data/v1/ei-support/`. No curve overlap % (Prepro-15 gap). |
| GUI-13 | EI precinct bar + CI | Preferred | **PASS** | MOCK-SEED | Synthetic fixtures in `mock-data/v1/ei-precinct-bar-ci/`. |
| GUI-15 | EI KDE results | Preferred | **PASS** | MOCK-SEED | Synthetic fixtures. Both states. |
| GUI-16 | Ensemble splits bar chart | Required | **PASS** | MOCK-SEED | Synthetic splits. Real API call. Both states. |
| GUI-17 | Box & Whisker | Required | **PASS** | MOCK-SEED | Synthetic data. Both race_blind and vra_constrained. Both states. |
| GUI-19 | Display interesting district plan | Preferred | **PARTIAL** | **MOCK** | `InterestingMap` exists; `useInterestingPlan` hook commented out at line 112. Uses hardcoded SC test data. |
| GUI-20 | VRA impact threshold table | Required | **PARTIAL** | MOCK-SEED | Synthetic threshold data (fake). Both states. Real SeaWulf-10 output not yet wired. |
| GUI-21 | ME box & whisker | Required | **PASS** | MOCK-SEED | Synthetic data. Merges rb+vra responses. Both states. |
| GUI-22 | ME histogram | Required | **PASS** | MOCK-SEED | Synthetic data. Both states. |
| GUI-23 | Reset page | Preferred | **PASS** | N/A | Clicking the "VRA Repeal Analysis" title in `CountryHeaderBar.jsx:25` calls `switchPage('Country')` and navigates to `/`, returning to the splash page. |
| GUI-26 | ME & majority-minority bar charts | Required | **PARTIAL** | **MOCK** | `Simulation.jsx:447–459` uses hardcoded `minorityEffectiveDistsData` constant. No API call made. |

---

## Preprocessing Use Case Coverage

| ID | Name | Priority | Status | Implementation | Notes |
|----|------|----------|--------|----------------|-------|
| Prepro-1 | Integrate multiple data sources | Required | **PASS** | `preproc6_2.py` | Merges Census PL 2020 (VAP), precinct boundaries, and election data via `maup`. GeoJSON output for both OR and SC. |
| Prepro-2 | Identify precinct neighbors | Required | **PASS** | `preproc6_2.py` | Builds neighbor graph via `gerrychain.Graph.from_geodataframe(adjacency='rook')`. Shared-boundary neighbors identified via GerryChain's spatial graph builder. |
| Prepro-3 | Integrate enacted plan with dataset | Required | **PASS** | `SeedDataLoader.java` + classpath TopoJSON | Enacted district topology seeded from `resources/topojson/`. District table (rep name, party, margin) seeded from `mock-data/v1/district-table/`. |
| Prepro-4 | Store preprocessed data | Required | **PASS** | MongoDB `cse416_braves` | 21+ collections seeded via `SeedDataLoader.java`. Geometry stored in classpath. Configurable via `APP_SEED_ROOT_PATH`. |
| Prepro-5 | Store SeaWulf data | Required | **PASS** | `SeedDataLoader.java` + `mock-data/` | SeaWulf output JSON files loaded from `APP_SEED_ROOT_PATH` and persisted to MongoDB collections (`ensemble_splits`, `box_whisker_results`, etc.). |
| Prepro-6 | Generate data files for SeaWulf | Required | **PASS** | `preproc6_2.py` | Outputs precinct adjacency graph JSON and `state_details.json` with `effective_district_count`. `gerrychain.py` reads the graph as `clean_graph.json` from the SeaWulf-staged directory. |
| Prepro-7 | Gingles precinct analysis | Required | **PASS** | `gingles_prepro_7_8.ipynb` | Produces per-precinct `minority_share`, `rep_vote_share`, `dem_vote_share` for 2024 Presidential. Output in `preprocessing/output/OR_2024_latino_gingles_scatter.json` and `SC_2024_black_gingles_scatter.json`. |
| Prepro-8 | Gingles non-linear regression | Required | **PASS** | `gingles_prepro_7_8.ipynb` | Non-linear regression curves for both Dem and Rep vote share. Regression curve points stored with scatter data and rendered in `GinglesScatterChart.jsx`. |
| Prepro-9 | PyEI ecological inference | Required | **PARTIAL** | `finalprepro_9.py` | Uses `pyei.two_by_two.TwoByTwoEI`. Runs for both OR and SC with all feasible groups. Output density curves seeded as synthetic fixtures — **actual PyEI JSON has not yet replaced mock-data fixtures for GUI display**. |
| Prepro-11 | Box & Whisker enacted plan | Required | **PASS** | `SeedDataLoader.java` | Enacted plan box-whisker points seeded into `box_whisker_results` via `buildEnactedBoxWhisker`. Both states, all feasible groups. |
| Prepro-12 | VRA-conscious analysis | Required | **MISSING** | `preproc12.py` (partial) | Script exists and computes per-district effectiveness scores. However, `groupRoughProportionality` output is **not yet seeded** into `state_summaries`, so `StatePage.jsx:112` renders empty. Effectiveness scores from this script also not yet in `district_table` documents. |

---

## SeaWulf Use Case Coverage

| ID | Name | Priority | Status | Implementation | Notes |
|----|------|----------|--------|----------------|-------|
| SW-1 | Server dispatcher | Required | **PASS** | `gerrychain.py` + SeaWulf SLURM | Protected directory at `/gpfs/projects/CSE416/Braves/team_data/{state_name}`. Data marshalled from Google Drive. Pre-staged `clean_graph.json` and `state_details.json`. |
| SW-2 | Run MGGG ReCom (race-blind) | Required | **PASS** | `gerrychain.py` (`VRA_ENABLED=False`) | MGGG GerryChain ReComProposal. Test ensemble (~250 plans) and large ensemble (~5,000 plans) both run. Population deviation configurable. |
| SW-3 | Run MGGG VRA Constrained ReCom | Required | **PASS** | `gerrychain.py` (`VRA_ENABLED=True`) | `enforce_vra_inclusion` constraint enforces minimum minority-effective district count from `state_details.json`. Both test and large ensembles run. |
| SW-4 | Coordinate multi-core SeaWulf | Required | **PASS** | `gerrychain.py:parallel_main()` | `ray` library coordinates parallel workers. `parallel_worker()` runs per-core chain; `ProgressTracker` (Ray remote class) tracks completion. Output sharded as `{prefix}_{core}.jsonl`. |
| SW-5 | Calculate election winners | Required | **PASS** | `gerrychain.py:make_updaters()` | `Election("PRES24", ...)` updater computes Democratic/Republican vote totals per district. `dem_votes` / `rep_votes` stored per plan step. |
| SW-6 | Calculate minority effectiveness score | Required | **PASS** | `gerrychain.py:calculate_effectiveness_score()` | `s_state`-based score: `recency × ei_conf × in_group_score × group_control`. Logit calibration via `prob_conf_conversion()`. Threshold ≥ 0.6 (`EFFECTIVENESS_SCORE_THRESHOLD`). |
| SW-7 | Calculate minority population % per district | Required | **PASS** | `gerrychain.py:make_updaters()` | Per-district minority population percentage calculated via `Tally` updaters for all `SIG_MINORITY_GROUPS`. Stored alongside effectiveness scores in plan output. |
| SW-8 | Calculate R/D split per plan | Required | **PASS** | `gerrychain.py:run_chain()` | Aggregates `D_Votes`/`R_Votes` per district per step. Split (dem seats vs. rep seats) computed by comparing totals. |
| SW-9 | Identify and store interesting plans | Preferred | **PASS** | `InterestingPlanDocument.java` + `SeedDataLoader.java` | Interesting plan topology and metadata stored in `interesting_plans` MongoDB collection. `GET /api/states/{stateId}/districts/interesting/list` and `?planId=` endpoints live. |
| SW-10 | Calculate ensemble measures | Required | **PASS** | `gerrychain.py` + `preproc5.py` | Per-plan: R/D splits, number of minority effective districts, majority-minority district count. Aggregated into `ensemble_splits`, `minority_effectiveness_histogram`, and `vra_impact_thresholds` collections. |
| SW-11 | Calculate box & whisker data | Required | **PASS** | `preproc5.py` | VAP-based box-whisker summaries (min/q1/median/q3/max) for all districts in each feasible group per ensemble type. Seeded into `box_whisker_results`. |
| SW-13 | Python profiler | Preferred | **PARTIAL** | Not committed | Profiling with Python `cProfile` was mentioned as implementing, but no profiling output script or results are present in the repo. The profiling may have been run locally without committing artifacts. |

---

## Mock Data Migration Priority List

### Priority 1 — Required use cases on hardcoded data (fix before presentation)

**GUI-26 — `Simulation.jsx:447–459`**
- Mock: `const minorityEffectiveDistsData = [{ label: "Latino", raceBlind: { min: 1, max: 3 }, vraConstrained: { min: 2, max: 4 } }]` — single hardcoded entry
- Real: `meBwData.groupSummaries` is already fetched at lines 422–423 via `useMeBoxWhiskerRb`/`useMeBoxWhiskerVra`. Each entry has `raceBlindSummary.min/max` and `vraConstrainedSummary.min/max`.
- Fix: Map `meBwData.groupSummaries` into `{ label, raceBlind: { min, max }, vraConstrained: { min, max } }` shape.

**GUI-4 — `MinorityHeatMap.jsx:63–71`**
- Mock: `const grades = [0, 5, 10, 20, 30, 40, 50]` drives legend and `percentageColor()` coloring. API bins fetched but ignored.
- Real: `heatmap.data.bins` from `useHeatmap()` contains `{ min, max, color }` per bin — already in scope.
- Fix: Use `bins` from API for both `LegendControl` rendering and the `featurePercent` color logic. Remove `percentageColor` and hardcoded `grades`.

**GUI-8 / GUI-19 — `Compare.jsx:185,258` and `Compare.jsx:112`**
- Mock: `SCCurrentDistrictData` (from `src/data/sc_test_compare_left.js`), `SCPackingData`, `SCCrackingData` imported at top. `testPlanList` hardcoded at lines 185–194. Dropdown renders `testPlanList` not `sortedPlanList`.
- Real: `GET /api/states/{stateId}/districts/interesting/list` and `GET /api/states/{stateId}/districts/interesting?planId=...` are live. `useInterestingPlanList` and `useInterestingPlan` hooks exist in `stateQueries.js`.
- Fix: Uncomment `useInterestingPlan` at line 112. Replace `testPlanList` with `sortedPlanList` in dropdown. Wire maps to real API. Remove 3 local JS file imports.

**Prepro-12 — `preproc12.py` output not seeded**
- Script computes per-district effectiveness and rough proportionality. Output not yet loaded into `state_summaries` — `StatePage.jsx:112` renders empty.
- Fix: Run script, add output to seed data, update `SeedDataLoader.java` to populate `groupRoughProportionality` in `StateSummaryDocument`.

### Priority 2 — Multi-group support blocked by `stateUtils.js`

**All analysis pages — `stateUtils.js:20`**
- Returns only `['Latino']` for OR and `['Black']` for SC. Backend serves Asian/White/Latino for OR and Black/Latino/White for SC.
- Fix: Change to `['Latino','Asian','White']` for OR and `['Black','Latino','White']` for SC. Single line unlocks multi-group selection across all pages.

### Priority 3 — Seeded synthetic fixtures (replace when SeaWulf/PyEI output available)

| Use Case | Mock Data Location | Replace When |
|----------|-------------------|--------------|
| GUI-12 | `mock-data/v1/ei-support/` | PyEI output (`finalprepro_9.py`) available |
| GUI-13 | `mock-data/v1/ei-precinct-bar-ci/` | PyEI precinct-level output available |
| GUI-15 | `mock-data/v1/ei-kde/` | PyEI KDE output available |
| GUI-16 | `mock-data/v1/ensemble-splits/OR_compare.json`, `SC_compare.json` | SeaWulf output available |
| GUI-17 | `mock-data/v1/box-whisker/*.json` | SeaWulf-11 output available |
| GUI-20 | `mock-data/v1/vra-impact-thresholds/*.json` | SeaWulf-10 output available |
| GUI-21, 22 | `mock-data/v1/minority-effectiveness-*/` | SeaWulf-6/10/11 output available |

### Already migrated to real preprocessing output
- **GUI-9, GUI-10:** `preprocessing/output/OR_2024_latino_gingles_scatter.json`, `SC_2024_black_gingles_scatter.json` — REAL

---

## Blockers (Required Use Cases Not Fully Implemented)

**BLOCKER — GUI-26 — Minority-effective and majority-minority bar charts**
- Requirement: "Display bar charts for the range of minority effective districts and the range of majority-minority districts for each feasible minority group. Bar charts should be available for race-blind and VRA-constrained ensembles."
- Missing: `Simulation.jsx:447–459` uses hardcoded single-entry constant. No API call made.
- Fix: Derive from `meBwData.groupSummaries` (already fetched). Majority-minority requires new SeaWulf data.

**BLOCKER — GUI-4 — Heatmap legend and coloring use hardcoded bins**
- Requirement: "Choose a number of bins that effectively shows the population distribution with bin ranges that are equal."
- Missing: `MinorityHeatMap.jsx:63` legend is hardcoded; `percentageColor()` uses hardcoded thresholds. API bin endpoint is fetched but ignored.
- Fix: Use `heatmap.data.bins` from API.

**BLOCKER — GUI-3 — Rough proportionality missing**
- Requirement: "The summary table will also contain the measure of rough proportionality for each feasible minority group."
- Missing: `StatePage.jsx:112` renders `<p className="statePageData statePageDataNum"></p>` — empty.
- Fix: Complete Prepro-12 output seeding.

**PARTIAL BLOCKER — GUI-6 — Per-group effectiveness scores missing**
- Requirement: Each row contains "the calculated effectiveness score and the calibrated effectiveness score for the district."
- Missing: Placeholder values only. Requires Prepro-13/14 output.

**PARTIAL BLOCKER — GUI-12 — Curve overlap % not displayed**
- Requirement: "Display the percentage overlap between the curves for each racial/language group."
- Missing: No overlap calculation or display. `polarizedVotingPercentage` not in seeded EI documents (Prepro-15 not done).

---

## Performance Findings

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| WARNING | `SeedDataLoader.java:1252–1272` | Individual `save()` calls in nested loops (16 saves for ME box-whisker) | Use `saveAll()` for bulk inserts |
| WARNING | `Compare.jsx:15–18` | 3 large topology objects imported into bundle via local JS files | Remove imports once real API is wired |
| INFO | `SeedDataLoader.java:968–975` | O(n²) `.stream().noneMatch()` in sampling fallback pass | Acceptable at 500-point target |
| INFO | `MinorityHeatMap.jsx:39` | `key={currMinority}` forces full layer remount on group change | Intentional Leaflet pattern; causes visible flash but is correct |
| INFO | `gerrychain.py:parallel_main()` | `ray` workers write to sharded JSONL files; no checkpointing | Acceptable for coursework; note if SeaWulf job preempted mid-run |

Note: `GeometryAssetService` correctly caches via `@Cacheable` + ETag (7-day max-age). `useMeBoxWhiskerRb`/`useMeBoxWhiskerVra` fire in parallel correctly.

---

## Error Handling Findings

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| WARNING | `Compare.jsx:40` | `onChangeTab` called in `InformationTable` click handler but never passed as prop → `TypeError` on district click | Add `onChangeTab` to `InformationTable` prop signature and pass from both call sites |
| WARNING | `Simulation.jsx:207–214` | `TooltipContent` accesses `.raceBlindSummary` without null check → crash if group summary is null | Add null guard before accessing `.min`, `.median`, etc. |
| INFO | `StateController.java:429` | `ensembleType` `@RequestParam` has no `@NotBlank` validation — empty string bypasses default | Add `@NotBlank` annotation |
| INFO | `ApiExceptionHandler.java:44` | Generic 500 handler suppresses exception from server logs entirely | Add `LOG.error("Unhandled exception", ex)` before building response |
| INFO | Frontend | TanStack Query default retry (3×) not disabled in `setupTests.js` — may cause slow/flaky tests | Add `queryClient.setDefaultOptions({ queries: { retry: false } })` in test setup |
| INFO | `gerrychain.py:enforce_vra_inclusion()` | If no feasible minority group meets threshold in initial partition, constraint raises `ValueError` with no retry | Wrap in try/except with fallback relaxation |

---

## Test Coverage Findings

### Backend — PARTIAL

- **`StateControllerTest.java`:** 5 tests covering topology routes, state summary, ensemble summary, and a broad smoke-test of all endpoints. Missing: error paths (missing group, invalid state), `getInterestingPlanList`, `listInterestingPlans`, `getMinorityEffectivenessBoxWhisker` with ensemble index variants.
- **`BackendDataService.java`:** No direct unit tests. `normalizeGroupSelector`, `requireFeasibleGroup`, `withStoredMetadata`, `normalizeParty` have no dedicated tests.
- **Payload invariant tests:** None assert `repWins + demWins = totalDistricts`, frequency sums, box-whisker ordering constraints, or VRA impact 3-row rule at the API response layer.
- **`GeometryAssetServiceTest.java`** and **`SeedDataLoaderTest.java`:** Present per file index.

### Frontend — PARTIAL

| Component | Tested |
|-----------|--------|
| `GinglesScatterChart.jsx` | ✓ (`GinglesScatterChart.test.jsx`) |
| `StatePage.jsx` | ✓ (`StatePage.test.jsx` — 2 tests) |
| `Simulation.jsx` | ✗ |
| `EI.jsx` | ✗ |
| `Compare.jsx` | ✗ |
| `MinorityHeatMap.jsx` | ✗ |
| `VRAAnalysis.jsx` | ✗ |
| `BoxWhiskerChart.jsx` | ✗ |
| `EiSupportChart.jsx` | ✗ |
| `SingleEnsembleSplitsChart.jsx` | ✗ |

---

## Code Quality & Coding Conventions Findings

### A. Modularity & Screen Readability

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| SMELL | `Simulation.jsx` (513 lines) | 15+ distinct components/functions in one file. `MinorityEffectivenessBoxWhisker`, `EnsembleSplits`, `VRAImpact`, etc. all defined here. | Split into per-chart files under `src/components/simulation/`. One component per file. |
| SMELL | `SeedDataLoader.java` (1,348 lines) | `samplePrecinctsForPlotting` (75 lines), `buildLockedGinglesChartPayload` (59 lines), `buildGinglesChartInternal` (64 lines) all exceed one screen. | Extract into `GinglesPayloadBuilder`, `GinglesValidator`, `EiPayloadSeeder`, etc. |
| SMELL | `Compare.jsx` (291 lines) | `CONGRESSIONAL_DATA`, `TEST_PACKING_DATA`, `TEST_CRACKING_DATA`, `testPlanList` defined inside component function body — re-created every render. | Move to module scope, or remove once real API is wired. |
| SMELL | `BackendDataService.java` | Constructor with 16 repository parameters (lines 43–77). | Group related repositories into a sub-record, or accept as standard Spring convention. |

### B. Data Structures

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| SMELL | `Gingles.jsx:95` | Fallback `gingles.data?.points` used as table rows — scatter points have `demVoteShare`/`repVoteShare`, not `republicanVotes`/`democraticVotes`. Table will render undefined. | Change to `const rows = ginglesTable.data?.rows ?? [];` |
| INFO | `GroupThresholds.java` | `Map.of()` is unordered; groups only looked up by key so no bug, but `LinkedHashMap` would be more explicit. | Minor — acceptable. |

### C. Naming Conventions

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| SMELL | `Simulation.jsx:235, 389` | `"Vra-Constrained Ensemble"` in Recharts legend `name` prop — "Vra" should be "VRA" | Change to `"VRA-Constrained Ensemble"` |
| MINOR | `Compare.jsx` | `CONGRESSIONAL_DATA` declared with `const` inside function body but uses ALL_CAPS | Move to module scope or rename to camelCase |

### D. Comments & Dead Code

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| SMELL | `Simulation.jsx:125`, `App.jsx:112,114,127,134,141` | Commented-out JSX blocks — dead code | Remove before presentation |
| SMELL | `Compare.jsx:94–122` | Entire `useEffect` + `useInterestingPlan` block commented out | Uncomment (it's the real implementation) or remove |
| MINOR | `MinorityHeatMap.jsx:66–71` | Real bins-driven legend commented out adjacent to hardcoded version | Implement (uncomment and fix) or remove |

### E. Magic Numbers

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| SMELL | `Simulation.jsx:348, 380` | `(group === "Latino" ? 6 : 7)` hardcodes district counts | Use `payload?.totalDistricts` from API data |
| SMELL | `MinorityHeatMap.jsx:63` | `[0, 5, 10, 20, 30, 40, 50]` hardcoded bin thresholds | Use `heatmap.data.bins` from API |
| SMELL | `SeedDataLoader.java:169–170` | Raw census population literals `4_272_371`, `5_478_831` | Promote to named class-level constants |
| SMELL | `SeedDataLoader.java:172–173` | Min/max population bound literals in validation method | Name as constants with explanatory comment |
| INFO | `SeedDataLoader.java:149` | `count() < 16` — magic number | Define `EXPECTED_ME_BOX_WHISKER_COUNT = 16` with comment: 2 states × 2 types × 4 indices |
| INFO | `gerrychain.py:68` | `VRA_ENABLED = True` toggled at script-top to switch run type | Promote to `sys.argv` parameter for consistency with `state_name` |

### F. Java-Specific Formatting

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| MINOR | `SeedDataLoader.java:140–142` | Multiple statements on one line | Split each statement to its own line |

### G. Intra-Language Consistency

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| SMELL | `stateUtils.js:20` | OR returns `['Latino']` only; SC returns `['Black']` only — but backend serves all feasible groups | Fix to `['Latino','Asian','White']` for OR and `['Black','Latino','White']` for SC |
| SMELL | `Compare.jsx` | Mixes TanStack Query hooks (left map via `useDistrictTopology`) with manual `useEffect`+`useState` (right map at lines 196–207) | Migrate right map to `useInterestingPlan` hook |
| SMELL | `App.jsx:52–60` | `minorityData` static array defined inside component — re-created every render | Move to module scope or derive from API |
| SMELL | `Gingles.jsx:95` | `isPolarized` hardcoded: `stateCode === "SC" ? true : false` | Should come from backend data (Prepro-15). Use `null` or omit until computed. |

### H. Configuration & Test Data

| Severity | Location | Issue | Suggested Fix |
|----------|----------|-------|---------------|
| SMELL | `SeedDataLoader.java` | OR/SC census population values hardcoded in Java | Move to property files or seed JSON |
| INFO | `MongoIndexConfig.java` | `MinorityEffectivenessBoxWhiskerDocument` index does not include `ensembleType` + `ensembleIndex` | Add `ensembleType` and `ensembleIndex` to the compound index |
| GOOD | `SeedDataLoader.java` | `APP_SEED_ENABLED` and `APP_SEED_ROOT_PATH` are configurable via env vars | No change needed |
| GOOD | `MongoIndexConfig.java` | All primary lookup fields have corresponding index definitions | No change needed |
| INFO | `gerrychain.py:46` | `VRA_ENABLED` is a script-level constant but `state_name` and `final_output_file` are `sys.argv` — inconsistent | Pass `VRA_ENABLED` as `sys.argv[4]` |

---

## Recommended Action Items

**Blockers — fix before presentation:**

1. **[GUI-26]** `Simulation.jsx:447–459` — Remove hardcoded `minorityEffectiveDistsData`. Derive `MinorityEffectiveDistrictsBar` from `meBwData.groupSummaries` (already fetched).
2. **[GUI-4]** `MinorityHeatMap.jsx:63–71` — Use `heatmap.data.bins` for both legend and coloring. Remove hardcoded `grades` and `percentageColor`.
3. **[GUI-8/19]** `Compare.jsx` — Uncomment `useInterestingPlan` (line 112), replace `testPlanList` with `sortedPlanList`, wire maps to real API, remove 3 local JS imports.
4. **[GUI-3/Prepro-12]** `StatePage.jsx:112` — Seed `groupRoughProportionality` from `preproc12.py` output and render in `EnsembleData`.

**High priority — fix before final submission:**

5. **[All pages]** `stateUtils.js:20` — Fix `groupOptionsForState` to full group lists. Single biggest multi-group unlock.
6. **[GUI-26]** `Simulation.jsx:348, 380` — Replace `(group === "Latino" ? 6 : 7)` with `payload?.totalDistricts`.
7. **[Compare.jsx:40]** Fix `onChangeTab is not a function` crash.
8. **[GUI-21]** `Simulation.jsx:207–214` — Add null guard in `TooltipContent`.
9. **[GUI-23]** Add reset button to `SplashPage` or `CountryHeaderBar`.
10. **[Gingles.jsx:95]** Fix fallback: `gingles.data?.points` → `[]` for table rows.

**Code quality:**

11. **[Simulation.jsx]** Split into per-chart files under `src/components/simulation/`.
12. **[Simulation.jsx:235, 389]** Fix `"Vra-Constrained"` → `"VRA-Constrained"`.
13. **[SeedDataLoader.java:140–142]** Split compound statements to separate lines.
14. **[MongoIndexConfig.java]** Add `ensembleType` + `ensembleIndex` to ME box-whisker compound index.
15. **[ApiExceptionHandler.java]** Add `LOG.error("Unhandled exception", ex)` before returning 500.
16. **[Tests]** Add: `MinorityEffectivenessBoxWhisker` null guard test, `EnsembleSplits` label merging test, `Compare` prop error test, backend test for `listInterestingPlans`.
